name: Update EPG

on:
  push:
    branches:
      - main
  schedule:
    - cron: '0 */6 * * *'  # Every 6 hours

jobs:
  update-epg:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # Step 2: Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      # Step 3: Install dependencies
      - name: Install dependencies
        run: npm install

      # Step 4: Run EPG editor script
      - name: Run EPG editor
        run: npm run edit

      # Step 5: Verify modified EPG exists
      - name: Verify modified EPG
        run: ls -l ./public

      # Step 6: Optional Debug - Print first 20 programmes in JSON
      - name: Debug: Print first 20 programmes
        run: |
          node -e "
          const fs = require('fs');
          const zlib = require('zlib');
          const xml2js = require('xml2js');
          const data = zlib.gunzipSync(fs.readFileSync('./public/epg6_modified.xml.gz'));
          xml2js.parseString(data, (err, result) => {
            if (err) throw err;
            console.log(JSON.stringify(result.tv.programme.slice(0, 20).map(p => ({
              channel: p.$?.channel,
              title: p.title?.[0],
              desc: p.desc?.[0],
              start: p.$?.start,
              stop: p.$?.stop
            })), null, 2));
          });
          "

      # Step 7: Commit and push updated EPG
      - name: Commit and push updated EPG
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: "Update EPG automatically"
          commit_user_name: github-actions[bot]
          commit_user_email: github-actions[bot]@users.noreply.github.com
          file_pattern: public/epg6_modified.xml.gz
          push: true
